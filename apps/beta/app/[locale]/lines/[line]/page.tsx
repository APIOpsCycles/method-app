import { notFound } from "next/navigation";
import { getCatalog, localePrefix, normalizeLocale } from "../../../public-method-data";
import { StationsSection } from "@apiops/design-system/react";
export default function Page({ params }: { params: { locale: string; line: string } }) { const locale = normalizeLocale(params.locale); const catalog = getCatalog(locale); const line = catalog.lines.find((item) => item.id === params.line); if (!line) notFound(); const prefix = localePrefix(locale); return <main className="public-content"><section className="public-hero"><p className="public-kicker">Metro line</p><h1>{line.title}</h1><p>{line.description}</p></section><StationsSection ordered title="Stations" items={line.stations.flatMap((stationId) => { const station = catalog.stations.find((item) => item.id === stationId); if (!station) return []; const cycles = catalog.cycles.filter((cycle) => cycle.stations.some((item) => item.id === stationId)); return [{ id: station.id, title: station.title, description: station.description, href: `${prefix}/stations/${station.id}`, relatedLinks: cycles.length ? cycles.map((cycle) => ({ href: `${prefix}/cycles/${cycle.slug}/stations/${station.id}`, label: `${cycle.title}: ${station.title}` })) : undefined }]; })} /></main>; }
