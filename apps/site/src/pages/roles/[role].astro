---
import RoleContent from "../../components/content/RoleContent.astro";
import BaseLayout from "../../layouts/BaseLayout.astro";
import { getCatalog, getRole, type Locale } from "../../lib/method-data";
export function getStaticPaths() { return getCatalog("en").routeProfiles.map((item) => ({ params: { role: item.id }, props: { locale: "en" as Locale } })); }
const { locale } = Astro.props;
const item = getRole(locale, Astro.params.role!);
if (!item) throw new Error("Unknown role");
const canonicalPath = `/roles/${item.id}`;
---
<BaseLayout title={item.title} description={item.summary} lang={locale} canonicalPath={canonicalPath}><RoleContent role={item} locale={locale} /></BaseLayout>
