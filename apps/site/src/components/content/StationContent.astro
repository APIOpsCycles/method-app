---
import { getCatalog, getLabels, getStationResources, localePrefix, type Locale, type Station } from "../../lib/method-data";
import EntityTools from "./EntityTools.astro";
import MethodMap from "./MethodMap.astro";
interface Props { station: Station; locale: Locale; cycle?: ReturnType<typeof getCatalog>["cycles"][number]; compatibilityAlias?: boolean }
const { station, locale, cycle, compatibilityAlias = false } = Astro.props;
const prefix = localePrefix(locale);
const labels = getLabels(locale);
const cycles = getCatalog(locale).cycles.filter((cycle) => cycle.stations.some((item) => item.id === station.id));
const selectedCycle = cycle ?? cycles[0];
const toolEntityId = cycle ? `${cycle.id}:${station.id}` : station.id;
const resources = getStationResources(locale, station);
---
<main class="public-content">
  <nav class="public-breadcrumbs" aria-label={labels["chrome.breadcrumb"]}><a href={`${prefix}/`}>APIOps Cycles</a><span aria-hidden="true">/</span>{cycle && <><a href={`${prefix}/cycles/${cycle.slug}`}>{cycle.title}</a><span aria-hidden="true">/</span></>}<span>{station.title}</span></nav>
  <section class="public-hero"><p class="public-kicker">{labels["entity.station"]}</p><h1>{station.title}</h1><p>{station.description}</p>{station.whyItMatters && <p>{station.whyItMatters}</p>}{compatibilityAlias && <p class="alias-notice">This compatibility URL presents the canonical station content.</p>}</section>
  <section class="public-section" aria-label={labels["map.selectedStation"]}><MethodMap locale={locale} cycleId={selectedCycle?.id} stationId={station.id} /></section>
  <section class="public-section" aria-labelledby="cycles"><h2 id="cycles">{labels["section.cycles"]}</h2><ul class="public-link-list">{cycles.map((cycle) => <li><a href={`${prefix}/cycles/${cycle.slug}`}>{cycle.title}</a><span>{cycle.description}</span></li>)}</ul></section>
  <section class="public-section" aria-labelledby="outcomes"><h2 id="outcomes">{labels["section.outcomes"]}</h2><ul>{station.outcomes.map((item) => <li>{item}</li>)}</ul></section>
  <section class="public-section" aria-labelledby="criteria"><h2 id="criteria">{labels["section.journeyCriteria"]}</h2><ul>{station.criteriaDetails.map((item) => <li>{item.title}</li>)}</ul></section>
  <section class="public-section" aria-labelledby="work"><h2 id="work">{labels["section.applyWork"]}</h2><p>{station.applyInWork}</p><ol>{station.steps.map((step) => <li>{step.text}{step.resourceId && step.resourceTitle && <> — <a href={`${prefix}/resources/${step.resourceId}`}>{step.resourceTitle}</a></>}</li>)}</ol></section>
  <section class="public-section" aria-labelledby="roles"><h2 id="roles">{labels["section.people"]}</h2><ul class="public-link-list">{station.stakeholders.map((role) => <li><a href={`${prefix}/roles/${role.id}`}>{role.title}</a><span>{role.description}</span></li>)}</ul></section>
  <section class="public-section" aria-labelledby="resources"><h2 id="resources">{labels["section.resources"]}</h2><ul class="public-link-list">{resources.map((resource) => <li><a href={`${prefix}/${resource.slug}`}>{resource.title}</a><span>{resource.description}</span></li>)}</ul></section>
  <EntityTools locale={locale} station={station} entityId={toolEntityId} />
  <section class="public-section" aria-labelledby="license"><h2 id="license">{labels["section.licensing"]}</h2><p>Reuse this community method content according to the <a href="https://github.com/APIOpsCycles/apiops-cycles-method-data/blob/main/LICENSE">method data license</a>.</p></section>
</main>
